apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: OCIRepository
metadata:
  name: keycloak
  namespace: keycloak
spec:
  interval: 15m
  url: oci://registry-1.docker.io/bitnamicharts/keycloak
  ref:
    semver: ">=24.0.0"
