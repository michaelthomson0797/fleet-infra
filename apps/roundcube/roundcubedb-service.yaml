apiVersion: v1
kind: Service
metadata:
  name: roundcubedb
  namespace: roundcube
  labels:
    service: roundcubedb
spec:
  ports:
  - port: 5432
    protocol: TCP
  selector:
    service: roundcubedb
