apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: michaelthomson
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: michaelthomson
  policy:
    semver:
      range: '>=0.1.0'
