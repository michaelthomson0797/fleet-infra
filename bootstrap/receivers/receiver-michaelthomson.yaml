apiVersion: notification.toolkit.fluxcd.io/v1
kind: Receiver
metadata:
  name: michaelthomson
  namespace: flux-system
spec:
  type: generic
  secretRef:
    name: webhook-token
  resources:
    - kind: ImageRepository
      name: michaelthomson
