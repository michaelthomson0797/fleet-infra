apiVersion: notification.toolkit.fluxcd.io/v1
kind: Receiver
metadata:
  name: michaelthomson
  namespace: flux-system
spec:
  type: github
  secretRef:
    name: webhook-token
  resources:
    - kind: ImageRepository
      name: michaelthomson
