apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: cnpg
  namespace: cnpg
spec:
  chart:
    spec:
      chart: cloudnative-pg
      version: 0.24.x
      sourceRef:
        kind: HelmRepository
        name: cnpg
        namespace: flux-system
  interval: 15m
  timeout: 5m
  releaseName: cnpg
  values:
