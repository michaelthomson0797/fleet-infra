apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cnpg
  namespace: cnpg
spec:
  chart:
    spec:
      chart: cloudnative-pg
      version: 0.24.x
      sourceRef:
        kind: HelmRepository
        name: cnpg
  interval: 15m
  releaseName: cnpg
  values:
