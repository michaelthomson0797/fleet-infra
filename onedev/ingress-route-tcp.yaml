apiVersion: traefik.containo.us/v1alpha1
kind: IngressRouteTCP
metadata:
  name: onedev-ssh
  namespace: onedev
spec:
  entryPoints:
    - onedev-ssh
  routes:
  - match: HostSNI(`*`)
    services:
    - name: onedev
      port: 22
